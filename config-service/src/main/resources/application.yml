server:
  port: 8088
spring:
  profiles:
    active: native
  config:
    server:
      git:
        uri: https://github.com/arkitdas/e-auction-app/tree/feature/cqrs-kafka/configs
        search-paths: '{application}'
        clone-on-start: true